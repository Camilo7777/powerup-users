server:
  port: 8081

spring:
  application:
    name: service-user
  datasource:
    url: jdbc:mysql://localhost:3306/user
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Pragma Powerup Users"
appVersion: "1.0.0"
